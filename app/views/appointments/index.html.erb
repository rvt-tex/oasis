<br><br><br>

    <% if current_client %>
    <div class="container mt-5">
        <div style="background: url(<%= asset_path('blue_bg.jpg') %>)" class="page-holder bg-cover">
            <article class="card-body mx-auto" style="max-width: 400px max-height: 800px;">
                <h2 class="card-title mt-3 text-center">Upcoming Appointments for <%= current_client.first_name %></h2>
                
                    <ul>
                            <% @appointments.each do |appointment| %>
                        <li>
                            <%= link_to appointment.treatment.title, appointment_path(appointment) %> 
                        </li>
                            <% end %>
                        <br><br>
                        <h5 class="card-title mt-3 text-center">We are looking forward to servicing you!!!</h5>
                    </ul>
                    <div class="container mt-5">
                        <div class="row">
                            <div class="col-lg-4">
                                <a class="btn btn-secondary" <%= link_to "Book a New Appointment »", new_appointment_path %></a>
                            </div>
                        </div>
                    </div>
            </article>
        </div>
    </div>

    <% end %>